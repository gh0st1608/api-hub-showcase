description: Retorna el estado de actualizacion de la contrasena.
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/model_response/StandardResponse.yaml
        - $ref: ../schemas/model_response/VerifyEmailRes.yaml
    examples:
      Password-Actualizado:
        summary: Contrasena actualizada correctamente
        value:
          Data:
            User:
              verifyEmail: true
            statusCode: 201
            message: Contrasena actualizado correctamente
