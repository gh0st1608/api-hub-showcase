type: object
description: Devuelve el identificador de la orden creada.
properties:
  Data:
    type: object
    properties:
      order:
        $ref: ../attributes/id.yaml
      statusCode:
        $ref: ../attributes/code.yaml
      message:
        $ref: ../attributes/message.yaml
    required:
      - order
      - statusCode
      - message
