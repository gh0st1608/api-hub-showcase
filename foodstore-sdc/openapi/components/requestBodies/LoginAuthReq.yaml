required: true
content:
  application/json:
    schema:
      $ref: ../schemas/model_request/LoginAuth.yaml
    examples:
      Login-Contrasena:
        summary: Login con email y contrasena
        value:
          Data:
            Auth:
              authType: password
              email: user@foodstore.com
              password: Password123!
              captchaToken: 03AGdBq24-example-token
