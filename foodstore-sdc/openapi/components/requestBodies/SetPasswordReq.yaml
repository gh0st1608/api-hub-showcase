required: true
content:
  application/json:
    schema:
      $ref: ../schemas/model_request/SetPassword.yaml
    examples:
      Definir-Contrasena:
        summary: Registrar nueva contrasena
        value:
          Data:
            User:
              email: user@foodstore.com
              password: NewPassword123!
